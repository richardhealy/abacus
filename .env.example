# abacus environment

# AI Gateway / provider credentials (used by real examples, not the offline demo).
# AI_GATEWAY_API_KEY=

# Redis connection for the budget store (added in the budgets milestone).
# REDIS_URL=redis://localhost:6379
